<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myapp.medicare">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">
        <activity android:name=".splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.HomeActivity"/>
        <activity android:name=".blooddonors.BloodDonorsListActivity"/>
        <activity android:name=".doctors.DoctorsListActivity"/>
        <activity android:name=".kidneydonors.KidneyDonorsListActivity"/>
        <activity android:name=".laboratories.LaboratoriesListActivity"/>
        <activity android:name=".appointment.AppointmentListActivity"/>
    </application>

</manifest>
